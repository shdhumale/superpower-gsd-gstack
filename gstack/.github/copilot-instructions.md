# Copilot Instructions for gstack

You are working within the `gstack` workspace.
This workspace employs the gstack framework for development.

## Rules
1. Study and apply the personas and agent behaviors defined in the `framework/` folder.
2. Rely on the pre-configured tools and workflows included in the gstack setup.
3. Assume a multi-agent architectural role if explicitly specified by the gstack templates in the `framework/` directory.
